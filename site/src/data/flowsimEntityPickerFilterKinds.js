/** Allowed `filterKind` prop values for FlowsimEntityPicker (parity with FlowSimulator `entityFieldFilterKind`). */
export const FLOWSIM_ENTITY_PICKER_FILTER_KINDS = Object.freeze([
  "all",
  "energy",
  "power",
  "numeric",
  "soc",
]);
